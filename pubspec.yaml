name: flutter_2024
description: "A Flutter 2014 project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  equatable: ^2.0.5
  dartz: ^0.10.1
  flutter_modular: ^6.3.2
  flutter_bloc: ^8.1.3
  connectivity_plus: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  bloc_test: ^9.1.5
  mocktail: ^1.0.3
  dartz_test: ^0.1.1

flutter:
  uses-material-design: true
